# arquivo .env
# variáveis de ambiente do backend da Clínica SEP
# ATENÇÃO: não envie este arquivo para o controle de versão (adicione ao .gitignore)

# configurações do banco de dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=@moOffice1
DB_NAME=clinica_sep

# chave secreta para assinar os tokens JWT
# IMPORTANTE: troque por uma string longa e aleatória em produção
JWT_SECRET=clinica_sep_jwt_secret_2024

# porta do servidor
PORT=3000
